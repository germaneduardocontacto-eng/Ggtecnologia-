Index.html



<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GG TEC | Servicio Técnico, Tips & Soluciones Smart TV</title>
    <!-- FontAwesome para íconos -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* --- ESTILOS GENERALES Y PALETA DE COLORES --- */
        :root {
            --bg-dark: #0A0A0C;
            --card-bg: rgba(22, 22, 28, 0.85);
            --border-glow: #FFFFFF;
            --text-main: #FFFFFF;
            --text-sub: #A0A0AA;
            --accent-glow: rgba(255, 255, 255, 0.15);
            --ml-yellow: #FFE600; /* Toque distintivo Mercado Libre */
        }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    }

    body {
        background-color: var(--bg-dark);
        color: var(--text-main);
        display: flex;
        justify-content: center;
        min-height: 100vh;
        overflow-x: hidden;
        background-image: 
            radial-gradient(circle at 50% 0%, rgba(255, 255, 255, 0.08) 0%, transparent 50%),
            radial-gradient(circle at 50% 100%, rgba(255, 255, 255, 0.03) 0%, transparent 50%);
    }

    /* --- ANIMACIÓN DE APERTURA ÉPICA (INTRO) --- */
    #intro-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: #000000;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        z-index: 9999;
        animation: fadeOutIntro 0.8s ease-in-out 1.8s forwards;
    }

    .intro-logo {
        width: 90px;
        height: auto;
        filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.8));
        animation: pulseGlow 1.2s infinite alternate ease-in-out;
    }

    .intro-text {
        margin-top: 20px;
        font-size: 0.9rem;
        letter-spacing: 4px;
        color: #888;
        text-transform: uppercase;
    }

    @keyframes pulseGlow {
        0% { transform: scale(0.95); opacity: 0.8; filter: drop-shadow(0 0 5px rgba(255, 255, 255, 0.4)); }
        100% { transform: scale(1.05); opacity: 1; filter: drop-shadow(0 0 25px rgba(255, 255, 255, 0.9)); }
    }

    @keyframes fadeOutIntro {
        0% { opacity: 1; visibility: visible; }
        100% { opacity: 0; visibility: hidden; }
    }

    /* --- CONTENEDOR PRINCIPAL --- */
    .container {
        width: 100%;
        max-width: 480px;
        padding: 30px 20px 50px 20px;
        display: flex;
        flex-direction: column;
        align-items: center;
        opacity: 0;
        animation: fadeInContent 0.8s ease-out 2s forwards;
    }

    @keyframes fadeInContent {
        to { opacity: 1; }
    }

    /* --- ENCABEZADO Y PROFILE --- */
    .header {
        text-align: center;
        margin-bottom: 25px;
    }

    .logo-frame {
        width: 90px;
        height: 160px;
        margin: 0 auto 15px auto;
        border: 3px solid #FFF;
        border-radius: 20px;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #000;
        box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        position: relative;
    }

    .logo-frame::before {
        content: '';
        position: absolute;
        top: 6px;
        width: 25px;
        height: 4px;
        background: #333;
        border-radius: 2px;
    }

    .brand-title {
        font-size: 1.8rem;
        font-weight: 800;
        letter-spacing: 1px;
        margin-bottom: 5px;
    }

    .brand-subtitle {
        font-size: 0.9rem;
        color: var(--text-sub);
        font-weight: 400;
        line-height: 1.4;
    }

    /* --- SECCIONES Y BOTONES --- */
    .section-title {
        width: 100%;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: var(--text-sub);
        margin: 20px 0 10px 5px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .section-title::after {
        content: '';
        flex: 1;
        height: 1px;
        background: rgba(255, 255, 255, 0.1);
    }

    .btn-card {
        width: 100%;
        background: var(--card-bg);
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 14px;
        padding: 16px 20px;
        margin-bottom: 12px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        text-decoration: none;
        color: var(--text-main);
        transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        backdrop-filter: blur(10px);
        position: relative;
        overflow: hidden;
    }

    .btn-card:hover {
        transform: translateY(-2px);
        border-color: rgba(255, 255, 255, 0.5);
        box-shadow: 0 5px 20px rgba(255, 255, 255, 0.15);
        background: rgba(35, 35, 45, 0.9);
    }

    .btn-content {
        display: flex;
        align-items: center;
        gap: 15px;
    }

    .btn-icon {
        font-size: 1.3rem;
        width: 25px;
        text-align: center;
    }

    .btn-text-main {
        font-weight: 600;
        font-size: 0.98rem;
        display: block;
    }

    .btn-text-sub {
        font-size: 0.78rem;
        color: var(--text-sub);
        margin-top: 2px;
    }

    /* Destacado Mercado Libre */
    .btn-ml {
        border: 1px solid rgba(255, 230, 0, 0.4);
        background: linear-gradient(135deg, rgba(255, 230, 0, 0.05) 0%, rgba(22, 22, 28, 0.9) 100%);
    }
    .btn-ml .btn-icon { color: var(--ml-yellow); }
    .btn-ml:hover { border-color: var(--ml-yellow); box-shadow: 0 5px 20px rgba(255, 230, 0, 0.2); }

    /* --- REDES SOCIALES --- */
    .social-grid {
        display: flex;
        justify-content: center;
        gap: 18px;
        margin-top: 10px;
        width: 100%;
    }

    .social-btn {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: var(--card-bg);
        border: 1px solid rgba(255, 255, 255, 0.15);
        display: flex;
        justify-content: center;
        align-items: center;
        color: #FFF;
        font-size: 1.2rem;
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .social-btn:hover {
        transform: scale(1.1);
        border-color: #FFF;
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
    }

    footer {
        margin-top: 35px;
        font-size: 0.75rem;
        color: var(--text-sub);
        text-align: center;
    }
</style>
</head>
<body>

<!-- PANTALLA DE APERTURA ÉPICA -->
<div id="intro-overlay">
    <div class="logo-frame intro-logo">
        <span style="font-size: 1.6rem; font-weight: 900; line-height: 1.1; text-align: center;">GG<br><span style="font-size: 1.1rem; font-weight: 400;">TEC</span></span>
    </div>
    <div class="intro-text">Cargando Experiencia...</div>
</div>

<!-- CONTENIDO PRINCIPAL -->
<div class="container">
    
    <!-- ENCABEZADO -->
    <header class="header">
        <div class="logo-frame">
            <span style="font-size: 1.6rem; font-weight: 900; line-height: 1.1; text-align: center;">GG<br><span style="font-size: 1.1rem; font-weight: 400;">TEC</span></span>
        </div>
        <h1 class="brand-title">GG TEC</h1>
        <p class="brand-subtitle">Servicio Técnico • Tips Android TV • Soluciones Digitales</p>
    </header>

    <!-- SECCIÓN 1: APPS Y CONTENIDO -->
    <div class="section-title">Contenido & Aplicaciones</div>

    <a href="#link-apps" class="btn-card">
        <div class="btn-content">
            <i class="fa-solid fa-mobile-screen-button btn-icon"></i>
            <div>
                <span class="btn-text-main">Apps Recomendadas</span>
                <span class="btn-text-sub">Descargas directas y tutoriales paso a paso</span>
            </div>
        </div>
        <i class="fa-solid fa-chevron-right" style="color: #555;"></i>
    </a>

    <a href="#link-tv-lx" class="btn-card" style="border-color: rgba(255,255,255,0.3);">
        <div class="btn-content">
            <i class="fa-solid fa-tv btn-icon" style="color: #FFF;"></i>
            <div>
                <span class="btn-text-main">TV. LX + Contenido Ilimitado</span>
                <span class="btn-text-sub">Acceso total a películas, series y canales</span>
            </div>
        </div>
        <i class="fa-solid fa-fire" style="color: #FFF;"></i>
    </a>

    <!-- SECCIÓN 2: MERCADO LIBRE Y TIENDA HIGH-END -->
    <div class="section-title">Productos Recomendados</div>

    <a href="https://listado.mercadolibre.cl" target="_blank" class="btn-card btn-ml">
        <div class="btn-content">
            <i class="fa-solid fa-bag-shopping btn-icon"></i>
            <div>
                <span class="btn-text-main">Mi Tienda en Mercado Libre</span>
                <span class="btn-text-sub">Equipos de alta gama, TV Boxes y accesorios probados</span>
            </div>
        </div>
        <i class="fa-solid fa-arrow-up-right-from-square" style="color: var(--ml-yellow); font-size: 0.8rem;"></i>
    </a>

    <!-- SECCIÓN 3: SERVICIOS Y ASESORÍA -->
    <div class="section-title">Servicio Técnico</div>

    <a href="https://wa.me/56912345678?text=Hola%20GG%20TEC,%20necesito%20una%20asesoría/reparación" target="_blank" class="btn-card">
        <div class="btn-content">
            <i class="fa-solid fa-wrench btn-icon"></i>
            <div>
                <span class="btn-text-main">Asesorías y Reparaciones</span>
                <span class="btn-text-sub">Agenda tu diagnóstico o soporte técnico directo</span>
            </div>
        </div>
        <i class="fa-brands fa-whatsapp" style="color: #25D366; font-size: 1.2rem;"></i>
    </a>

    <!-- SECCIÓN 4: REDES SOCIALES -->
    <div class="section-title">Comunidad & Redes</div>

    <div class="social-grid">
        <a href="https://youtube.com" target="_blank" class="social-btn" title="YouTube">
            <i class="fa-brands fa-youtube"></i>
        </a>
        <a href="https://tiktok.com" target="_blank" class="social-btn" title="TikTok">
            <i class="fa-brands fa-tiktok"></i>
        </a>
        <a href="https://facebook.com" target="_blank" class="social-btn" title="Facebook">
            <i class="fa-brands fa-facebook-f"></i>
        </a>
        <a href="https://instagram.com" target="_blank" class="social-btn" title="Instagram">
            <i class="fa-brands fa-instagram"></i>
        </a>
    </div>

    <footer>
        © 2026 GG TEC • Todos los derechos reservados.
    </footer>

</div>
</body>
</html>
